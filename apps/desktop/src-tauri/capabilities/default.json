{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop window permissions.",
  "windows": ["actor-*", "settings"],
  "permissions": ["core:default", "dialog:default"]
}
