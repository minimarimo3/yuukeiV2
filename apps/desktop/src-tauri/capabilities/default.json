{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop window permissions.",
  "windows": ["actor-*", "settings", "stage-overlay-*", "startup-error"],
  "permissions": ["core:default", "dialog:default"]
}
