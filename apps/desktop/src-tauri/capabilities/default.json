{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop window permissions.",
  "windows": ["actor-*", "bubble-*", "settings", "startup-error"],
  "permissions": ["core:default", "dialog:default"]
}
